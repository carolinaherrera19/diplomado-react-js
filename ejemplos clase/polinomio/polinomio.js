function polimonio(x) {
    y = math.pow(x, 2)    
    respuesta = (y + x + 1)
    
    return respuesta

    x = parseInt(window.prompt("ingresa el numero"))

    console.log(" es : " + respuesta(x))
}
polimonio()